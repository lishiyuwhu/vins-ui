# TODO — vins-gateway integration follow-ups

Captured during Phase 1 sidebar persistence work
(`feat/sidebar-persistence`). These items were intentionally deferred to
keep Phase 1 reviewable.

## High value
- [ ] Pagination: cursor-based "load more" for `GET /web/conversations`
- [ ] Pagination: cursor-based "load more" for `GET /web/conversations/:id/messages`
- [ ] Optimistic UI: write user message to `POST /web/conversations/:id/messages`
      on submit, then match the assistant reply by `task_id` when polling
      completes (avoids gateway best-effort write race)
- [ ] Bind locally-created conversations to DB rows: when a `local-...`
      conversation later appears in the next `GET /web/conversations`
      response with the same `session_id`, merge instead of duplicating
      (Q-C in plan — accepted as known visual duplicate for v1)

## Medium
- [ ] Inline rename UI (replace `window.prompt`)
- [ ] Confirm-dialog modal styled to match the app (replace `window.confirm`)
- [ ] Per-message delete via `POST /web/conversations/:id/messages/:msgId/delete`
- [ ] Re-hydrate richer fields (`originalImageUrl`, `recommendations`,
      `recommendation_status`) from `/api/v1/agent/conversations/:sid`
      when the user opens a DB-restored conversation that has a `sessionId`
- [ ] Empty-title placeholder: show "未命名对话" until first user message
      arrives (`Conversation.title` is auto-generated by gateway)
- [ ] Sidebar reordering by `last_message_at` after each turn completes
      (currently order is the snapshot from the last list fetch)
- [ ] Toast for rename / delete success/failure (currently `window.alert`)

## Low
- [ ] Access Token UI (`/web/me/token` GET, `/web/me/token/reset` POST)
- [ ] Surface `error_message` from failed assistant DB messages with a
      retry affordance
- [ ] Sync `dismissedRecommendationIds` to local storage so dismissals
      survive reload (currently lost because not in DB)

## Notes
- DB schema reference: `temp/api-reference.md`
- Migration spec: `temp/UI_adapt.md`
- Phase 1 ships: list+restore+resume polling+rename+delete only.
